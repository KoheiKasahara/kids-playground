# 「こっきピンボール」基本設計

## 概要

国旗そのものが球に見える「国旗ボール」を3個選んで打ち出し、盤面のバンパー・ピンに当たりながら落ちていく先の得点ゾーン（100〜1000点）で得点を競うミニゲームです。正解・不正解のないアクション寄りの遊びで、国旗を選ぶ・見分ける楽しさに焦点を当てています。盤面の見た目は4テーマから選べ、タップで反応する回転おもちゃと押し上げおもちゃも置きます。物理演算には[matter-js](https://brm.io/matter-js/)を使い、盤面はヘッドレスで計算した座標をDOMへ直接反映して描画します。問題データ・盤面データはすべてアプリに同梱し、初回読み込み後もPWAのキャッシュで遊べます。

## 画面と進行（URL）

こっきピンボールに難易度選択はありません。

| 画面 | URL |
| --- | --- |
| ボール選択 | `/games/flag-pinball` |
| プレイ | `/games/flag-pinball/play` |
| 結果 | `/games/flag-pinball/result` |

```text
ホーム → ボール選択（3個選ぶ）→ プレイ（3球射出）→ 結果 → もういちど／ボールをかえる
```

プレイ・結果の各画面は`useLocation().state`（選んだ`flagIds`、結果は確定`scores`も）で前の画面から情報を受け取ります。型ガード`isPinballPlayState`/`isPinballResultState`（`src/games/flag-pinball/playState.ts`）で形を検証し、不正・stateなしのURL直打ちはボール選択画面へ`replace`で戻します。

再プレイ時に前回の物理状態・得点が残らないことは、プレイ画面の内部コンポーネント（`PinballGame`）に`key={location.key}`を付けて丸ごと作り直す構造で保証しています（`useState`のリセットや`usePinballEngine`の再構築を個別に気にする必要がありません）。

## テーマ

盤面の見た目は次の4テーマから選べます。`PINBALL_THEMES`の登録順がそのまま選択UIの循環順になります。

| テーマ | 表示名・アイコン |
| --- | --- |
| `normal` | 🌈 ノーマル |
| `space` | 🚀 うちゅう |
| `ocean` | 🌊 うみ |
| `candy` | 🍭 おかし |

テーマが変えるのは、盤面の背景・壁・ガイド壁・ゾーン仕切り・バンパー・ピン・得点ゾーン・得点ポップの配色、背景の軽い装飾、おもちゃの絵と発動時のアニメーションに加えて、`boardConfigs/`（後述）が持つテーマ別のピン・バンパー・壁・おもちゃ配置です。Phase Aでこの盤面配置をテーマごとに分離し、Phase Bで宇宙テーマ（`space`）、Phase Cで海テーマ（`ocean`）をそれぞれ専用の配置へ変更しました。ノーマル・おかしの2テーマは今も同じ配置（`normalBoard`と内容が一致する独立コピー）を使い、プレイ感は変わっていません。国旗ボールの選択内容、通常モードと全射出モードの進行、射出間隔のルール、盤面の論理座標系、得点ゾーンの割り当てはすべてのテーマで共通です。「## テーマ別の盤面配置（boardConfigs）」で詳しく説明します。

実装では、テーマごとに`normalTheme.module.css`/`spaceTheme.module.css`/`oceanTheme.module.css`/`candyTheme.module.css`を独立して持ちます。`PinballBoard`の論理盤面へテーマのクラスを付け、盤面側のCSSが参照するCSS変数をテーマ側で上書きするだけで配色を切り替えます。座標や物理BodyをテーマCSSから変更する仕組みはありません。CSS変数の既定値は現在の見た目と同じで、ノーマルテーマは従来と同じ盤面になります。

テーマ定義（`PinballThemeDefinition`）が持つのは、id・選択UI用のひらがな表示名・絵文字、盤面とおもちゃのクラス名、任意の背景装飾、種類ごとのおもちゃの見た目を返す`renderToy`だけです。盤面座標・得点・物理パラメータをテーマ定義へ持たせない責務分離にしており、おもちゃの`renderToy`も物理を持たず絵だけを返します。

選択UI（`PinballThemePicker`）は、左右の64px以上の丸いボタンで前後のテーマを循環させる単純な操作です。現在のテーマには絵文字と名前を表示し、盤面と同じCSS変数を使う小さなプレビューにバンパーとピンも描きます。テーマを選んだ瞬間に`kids-playground:pinball-theme`をキーとして`normal`/`space`/`ocean`/`candy`のidを`localStorage`へ保存します。未設定・空文字・未知のidや読み取り例外はノーマルへフォールバックします。保存時の例外で書き込めない場合も、画面上はメモリ内の選択で切り替えられます。

## 国旗ボール75種と円形描画の方針

`src/games/flag-pinball/data/pinballFlags.ts`の`PINBALL_FLAG_IDS`（実体は`components/flag-ball/flagBalls.ts`）が、選択画面に並べる75か国を定義します。大半は既存の国旗クイズ（`flag-quiz`）の`Country`データをそのまま`PinballFlag`として再利用していますが、北マケドニアとブルガリアの2か国だけは105か国マスターに含まれないため`flag-ball/flagBalls.ts`内で個別に追加しています（新しい静的素材の追加はブルガリアのSVG1件のみ）。75か国は、丸くクロップしても模様の特徴（縞・十字など）が一目で分かる国だけを選んでいます。

`FlagBall`コンポーネントは、国旗SVG（4:3）を`object-fit: cover`で正方形へ収めてから円形にクロップします。`cover`は縦横同じ倍率で拡大してから溢れた分を切るため国旗の縦横比は保たれますが、高さが基準になる分、左右が絵の幅の12.5%ずつ切れます。端に意匠がある国旗（シンガポール・ネパール・モンゴル・トンガ）だけは`ballPositionX`で左端寄せにして、主要な模様が欠けないようにしています。全75件のSVGが`viewBox="0 0 640 480"`であることは`components/flag-ball/flagBalls.test.ts`で検証しています。

## 盤面と物理パラメータ

盤面は論理座標480×1000（縦横比0.48）で固定し（`boardLayout.ts`）、実機の画面サイズには依存させません。表示側（`useBoardScale`）が利用できる幅・高さから倍率を求めてCSS transformで拡縮するだけなので、物理パラメータ（反発係数・初速など）はこの1つの座標系だけで調整すれば、どの端末でも同じ挙動になります。倍率に下限は設けていません。極端に低い横画面などでは盤面全体が小さく表示されますが、`overflow: hidden`で見切れるより望ましいと判断しています。

ピン・バンパー・壁・おもちゃ・射出パラメータの配置は`boardConfigs/`以下でテーマごとに持ちます（`BoardConfig`型、`boardConfigs/types.ts`）。`normal`/`candy`は同じ内容の独立コピーで、以下で説明する数値はこの共通の基準盤面（`normalBoard.ts`）のものです。`space`はPhase Bで、`ocean`はPhase Cでそれぞれ専用配置（`spaceBoard.ts`/`oceanBoard.ts`）に置き換えており、詳しくは「宇宙盤面（spaceBoard）とジャンプ台toy」「海盤面（oceanBoard）とシーソーtoy」で説明します。

基準盤面にはバンパー3個（半径28）・ピン31個（半径8）の計34個（`OBSTACLES`）と、外壁・射出ガイド壁・ゾーン仕切り（`WALLS`/`ZONE_DIVIDERS`）を配置します。ピンは次の7段の千鳥配置です。

| y | 本数 | x座標 |
| ---: | ---: | --- |
| 130 | 5 | 70, 155, 240, 325, 410 |
| 210 | 4 | 112.5, 197.5, 282.5, 367.5 |
| 280 | 5 | 70, 155, 240, 325, 410 |
| 480 | 4 | 112.5, 197.5, 282.5, 367.5 |
| 555 | 5 | 70, 155, 240, 325, 410 |
| 735 | 3 | 155, 240, 325 |
| 820 | 5 | 70, 155, 240, 325, 410 |

バンパーは中央(240,385)、左下(90,655)、右下(390,655)です。下側の3本ピン段はバンパーと同じyに重ねず、横一列の実質的な壁を作らないようにしています。障害物どうしの中心距離は「障害物の半径の和＋ボール直径＋16px」以上を保ちます。最終配置の最小マージンは17.8917pxで、ボールが詰まらず素直に通り抜けられることを`boardLayout.test.ts`で検証しています。

国旗ボールの半径は24で、盤面幅の1割を占めます。4〜5歳が3球を同時に目で追え、転がる国旗の模様まで見分けられることを優先し、そのぶん障害物間の余白とのバランスを取っています。物理演算は`usePinballEngine`が60fps固定のタイムステップ＋rAFでmatter-jsのEngineだけをヘッドレスに回し、ボールの座標をReactの再レンダーを介さず対象DOM要素へ直接`transform`で書き込みます（3球同時進行でもフレーム落ちしにくくするため）。速度・角速度には上限（`MAX_SPEED`/`MAX_ANGULAR_VELOCITY`）を設け、薄い壁のすり抜けや国旗が読めなくなるほどの高速回転を防いでいます。

ボールが一定時間（1.5秒）ほぼ静止したままだと「停滞」とみなし、`Body.setVelocity`で控えめな速さを直接与えて小さく突きます。`Body.applyForce`は内部でタイムステップの二乗が掛かるため、狙った速さを直接指定できる`setVelocity`を使っています。

3球は`LAUNCH_DELAYS_MS`（0/350/700ms）で少しずつ時間差射出し、常に複数球が同時に動く状態を作ります。射出位置・初速には毎回小さな揺らぎ（射出位置±30px、x方向-5〜5px/step、y方向6〜10px/step）を持たせ、同じ軌道の繰り返しを避けています。遅延は1球の落下時間より短くしています。

## おもちゃの配置と物理

盤面には、回転するおもちゃを左右対称に2個、ボールを押し上げるおもちゃを1個置きます。配置はテーマや物理実装から分離し、`toyLayout.ts`の論理座標へ固定しています。

| おもちゃ | id / 種類 | 中心座標 | 見た目の半径 | タップ判定半径 |
| --- | --- | ---: | ---: | ---: |
| 回転（左） | `toy-spinner-left` / `spinner` | (110, 385) | 37px | 56px |
| 回転（右） | `toy-spinner-right` / `spinner` | (370, 385) | 37px | 56px |
| 押し上げ | `toy-launcher` / `launcher` | (240, 645) | 30px | 56px |

回転おもちゃは中央バンパー（240,385）を挟んで左右対称に置き、右Toyのxは`BOARD_WIDTH - 左ToyのX`（370 = 480 - 110）で導出しています。左右は`toyLayout.ts`の同じ半径・タップ判定半径の定数を共有し、`createSpinnerToy`（`spinnerToy.ts`）という同じ共通ランタイムから独立したインスタンスとして生成されるため、性能差やコードの重複がありません。押し上げおもちゃは下側の左右バンパー（90,655）と下段へ続くピンの間の中央に置いています。どのおもちゃも既存のピン・バンパーとの中心距離を「おもちゃの半径＋障害物の半径＋ボール直径48px」以上に取り、ボールが通れる余裕を残しています（回転おもちゃに最も近い障害物はpeg-row-4のピンで、これ以上半径を大きくするとこの制約を割り込みます）。回転おもちゃの羽根も厚さ13pxの細い十字にして隙間を残します。`toyLayout.test.ts`で盤面内にボール半径ぶんの余裕があること、全障害物との距離、タップ判定円どうしの非重複に加えて、左右Toyが盤面中心に対する鏡写しの位置にあり、サイズ・タップ判定に差がないことを検証しています。

いずれも得点ゾーン上端`ZONE_TOP=875`より上に収め、回転おもちゃは`y+radius=422`、押し上げおもちゃは`y+radius=675`なので得点ゾーンから離れています。タップ判定半径はどのおもちゃも56px（直径112px）で、盤面を実用下限の0.70倍に縮小しても直径78.4pxあります。主操作としてタップしやすい大きさを確保し、3つの判定円も重ならないようにしています。得点ゾーンへ向かうボールの自然な流れを塞がないことは、障害物間隔の検証に加えて、回転中もおもちゃの真上のボールが下の得点ゾーン方向へ落ち続けること、押し上げを毎フレーム発動しても有限時間で得点ゾーンの高さへ到達することを物理テストで確認しています。

おもちゃのテーマごとの見た目は次の通りです。

| おもちゃ | 🌈ノーマル | 🚀うちゅう | 🌊うみ | 🍭おかし |
| --- | --- | --- | --- | --- |
| 回転 | 4枚羽根＋ハブ | 十字のソーラーパネル衛星 | 4本の十字スクリュー | 十字のキャンディ風車 |
| 押し上げ | バネバンパー | ロケット噴射 | クジラの潮吹き | ポップコーン |
| ジャンプ台※1 | バネ板（簡易） | ロケット発射台 | 泡パッド（簡易） | グミパッド（簡易） |
| シーソー※2 | 木の板＋岩（簡易） | 木の板＋岩（簡易） | 岩の上で傾く木の板 | 木の板＋岩（簡易） |

※1 ジャンプ台はPhase Bで追加したおもちゃの種類です。Phase B時点では宇宙盤面だけに実際に配置されており、ノーマル・海・おかしの絵は「テーマ定義は全種類の絵を返す」という既存の不変条件（`themes.test.ts`）を満たすための簡素な代替です。
※2 シーソーはPhase Cで追加したおもちゃの種類です。海盤面だけに実際に配置されており、ノーマル・宇宙・おかしの絵は同じ不変条件を満たすための簡素な代替です。

回転おもちゃは円形ディスクではなく、Matterのcompound bodyと同じ4方向の十字型です。共通CSSの`.toyVisual[data-toy-kind='spinner']`は背景・枠・影を外し、羽根の長さは半径×2の`74px`、厚さは`13px`です（長さ・厚さとも同じ比率で大きくしており、長いだけの細い羽根になりません）。共通CSSの`--toy-blade-thickness: 17.57%`（13/74）と`--toy-blade-length: 50%`を4テーマが参照し、北・南は「厚さ×長さ」、東・西は「長さ×厚さ」として、対向する羽根でMatterの十字と同じ寸法を作ります。左右2個の回転おもちゃは同じ半径を使うため、この比率は共通で成立します。4枚の羽根はテーマ内で色を変えており、回転中も位相を読み取りやすくしています。

回転は外側の`.toyVisual`にある`rotate(var(--toy-spin))`の1箇所だけで行い、テーマ側はspinner全体の`transform`を上書きしません。そのため、描画の中心・回転角とMatter Bodyの中心・角度が一致します。押し上げおもちゃは有効時間中に控えめな待機リングを`box-shadow`で表示します。押し上げおもちゃの拡大は、`ToyVisualState.scale`から`usePinballEngine`を経由してDOMの`--toy-scale`へ渡され、共通CSSの`transform: rotate(var(--toy-spin)) scale(calc(var(--toy-scale) * (1 + 0.12 * var(--toy-pulse))))`に反映されます。見た目の拡大とMatter Bodyの拡大を同じ状態から適用するため、空振りでもタップの手応えが見えます。

物理とテーマは分離しています。回転の物理は`spinnerToy.ts`、押し上げの物理は`launcherToy.ts`にそれぞれ共通実装を1つだけ持ち、テーマごとに物理コードを複製していません。テーマ側は種類ごとの絵と色・アニメーションだけを返します。エンジンとの接続点は`toyRuntime.ts`の`ToyRuntime`という1つの共通インターフェースです。`usePinballEngine`は`TOYS`からランタイムを作って物理Bodyを同じWorldへ追加し、タップ時に`activate`、毎フレームに有効なボールを渡して`update`、`readVisualState`をDOMのCSS変数へ反映します。cleanupではアクティブな実行トークンを無効化し、ランタイムMap・rAF・射出タイマー・衝突イベント・MatterのWorldを片付けるため、前のプレイのおもちゃが次のプレイへ残りません。

回転おもちゃをタップすると、子どもがあとから来るボールも回転へ間に合う長さとして発動時間を`SPINNER_ACTIVE_DURATION_MS=2600ms`にします。角速度は発動開始からの経過時間ではなく残り時間だけで決め、`rotationVelocity = SPINNER_MAX_ANGULAR_VELOCITY * clamp(残り時間 / SPINNER_SPIN_DOWN_MS, 0, 1)`です。`SPINNER_SPIN_DOWN_MS=700ms`なので、残り700msを切るまでは上限を保ち、最後の700msだけ滑らかに減速します。上限は`SPINNER_MAX_ANGULAR_VELOCITY=Math.min(MAX_ANGULAR_VELOCITY*0.75, 0.16)=0.16rad/step`です。発動終了時刻は各タップの`now+2600ms`を上限に更新するため、再タップは残り時間を増やして角速度を必ず上限へ戻しますが、効果時間や角速度を加算し続けません。見た目のパルスは発動時間から分離した`320ms`です。実測ではタップから`0.16rad/step×60=9.6rad/s`（約1.5回転/秒）で回り続け、1.5秒・1.7秒・1.9秒の再タップでも速度は9.6rad/sのまま変わらず、最後のタップから2600ms後の700ms前から減速して停止しました。羽根の接触直前のボールにも穏やかな補助を届ける作用範囲は中心から`37+BALL_RADIUS(24)+8=69px`です（左右どちらの回転おもちゃも同じ半径・作用範囲を共有します）。回転中にこの範囲へ入ったボールの速さが`SPINNER_BALL_SPEED_CAP=10px/step`を超えると、ボール直径48pxの初速上限24px/stepの半分以下に抑えるため、向きを保ったまま10px/step以下へ縮めます。さらに速さが`0.35px/step`未満のボールには、停滞を解除する小さなきっかけとして接線方向の`2.4px/step`を`Body.setVelocity`で直接設定しますが、毎回の加速にならないよう同じボールには`200ms`のクールダウンを置きます。タップしていないときにボールが新しく作用範囲へ入った場合も、存在感を残すため`420ms`、角速度`0.022rad/step`の短い受け身回転だけを行います。

押し上げおもちゃをタップすると、近くにいたボールへその瞬間だけ1回作用するのではなく、`LAUNCHER_ARMED_DURATION_MS=2000ms`の有効（armed）状態になります。有効時間中は毎フレーム作用条件を確認するため、タップよりあとに来たボールも対象になり、ピンポイントのタイミングを必要としません。全体クールダウン`PHYSICS_COOLDOWN_MS=360ms`は廃止し、有効時間がその役割を担います。同じボールへの個別クールダウン`1200ms`は維持します。見た目のパルスは`300ms`で有効時間とは分離しており、空振りでもタップすれば拡大とパルスが出ます。有効中は見た目とMatter Bodyの両方を`LAUNCHER_ARMED_SCALE=1.15`倍にし、状態が変わったフレームだけ`Matter.Body.scale`を呼んで、現在の適用倍率との比を渡します。そのため有効・非有効を往復しても倍率の誤差が累積せず、描画だけ大きくなって当たり判定がずれることもありません。おもちゃの半径は`30px`なので、拡大時は`34.5px`になります。

おもちゃの半径とボール半径に余裕を足した近距離だけを対象にするため、作用範囲は中心から`LAUNCHER_INFLUENCE_RADIUS=96px`以内です。拡大時のおもちゃ半径`34.5px`とボール半径`24px`の接触距離`58.5px`に余裕を残しつつ、長距離から拾う不自然さを抑えています。得点直前のボールを拾わないため、おもちゃの中心y=645より30px以上上（`y<615`）ではないボールだけを対象にし、すでに上向き速度が`-11.5px/step`以下のボールも対象外です。対象ボールには上向き初速`-11.5px/step`を設定します。重力`0.55px/step`での上昇量は`11.5^2/(2*0.55)≒120px`で、ボール直径約48pxの2.5個分です。真上へ固定せず盤面中央へ寄せ続けないため、既存の横速度を25%だけ残して左右へ`0.8〜1.8px/step`のランダムな散らしを加え、横速度を`±4px/step`以内に収めます。異常値が入っても既存の全体上限を超えないよう、合成速度には`LAUNCH_SPEED_CAP=Math.min(MAX_SPEED*0.55, 13)=13px/step`を設けています。

おもちゃの安全装置は、速度を力として加算せず`Body.setVelocity`で設定することを基本にしています。回転おもちゃは回転中だけ近傍ボールの速度を10px/step以下へ制限し、押し上げおもちゃは上向き速度を11.5px/step、合成速度を13px/step以下にします。通常のエンジン側にもボール全体の速度上限`MAX_SPEED=24px/step`があります。作用範囲は中心からの距離で限定し、押し上げはおもちゃより上のボールを対象にせず、同じボールへの`1200ms`クールダウンも設けています。回転おもちゃの上限処理は`rotationVelocity !== 0`のときだけなので、羽根が完全停止しているときは既存ボールの速度を制限しません。おもちゃに触れないボールへ新しい速度を足すこともなく、従来の挙動を保ちます。

おもちゃは攻略必須ではありません。何も操作しなくても、従来どおり3球が最後まで得点確定してゲームが成立します。タップは落下のきっかけや見た目の反応を加える任意の遊びです。

## 宇宙盤面（spaceBoard）とジャンプ台toy

Phase Bで、宇宙テーマ（`space`）だけに専用の盤面配置（`boardConfigs/spaceBoard.ts`）を作りました。通常テーマ（王道のピンボール）はそのまま残し、宇宙テーマは「重力に振り回されながら、惑星や人工衛星の間を飛び回るような盤面」を狙っています。基本の重力・ボール物理・得点ルール・射出タイミングは共通のまま、配置だけで軌道の印象を変えています。

障害物は基準盤面の34個よりずっと少ない10個（振り分けピン1・惑星バンパー2・ゴール手前の千鳥ピン4＋端のピン2・ゴール直前の小型バンパー1）に絞り、代わりに長さ260pxの斜めガイド壁2枚（`wall-space-ramp-a`/`wall-space-ramp-b`）で盤面を大きく横断させます。上部は中央の振り分けピンで弱く左右へ振ってから、離れた位置にある2個の惑星バンパー（半径26、x=110/370）へ届かせて「上部：左右へ散らす」を作ります。中盤の2枚の斜めガイド壁は、高さ(y)も中心xも大きくずらしており（`spaceBoard.test.ts`で検証）、2枚が交わる隙間にボールが挟まる罠を作りません。下部の配置は「ゴール直前の進入経路」で説明します。

回転おもちゃ（`spinner`）は左右対称2個ではなく1個だけを「人工衛星」として配置し、障害物総数を抑えています。既存の`spinnerToy.ts`をそのまま再利用し、テーマ側の絵だけを変えています（既存の拡張ポイントの実例）。

### ジャンプ台（`jumppad`）

宇宙テーマ専用の新しいおもちゃとして「ジャンプ台」（`toy-jumppad-rocket`）を追加しました。他の2種類（回転・押し上げ）と違い、タップで発動する仕掛けではなく、ボールが触れるだけで自動的に作動します。宇宙盤面のどこでボールが近づいても跳ね返る必要があるため、タップ待ちにしていません（`jumppadToy.ts`）。

- 接触判定はおもちゃ半径＋ボール半径＋10pxの範囲。範囲内のボールへ`Body.setVelocity`で直接、上向き13px/step・横方向3〜7px/stepをランダムな向きへ与えます（`applyForce`はタイムステップの二乗で効き方が変わるため使わず、他のtoyと同じく`setVelocity`で直接指定）。
- 上昇量は重力0.55px/stepから逆算して約153.6px（ボール直径の3.2個分）。押し上げおもちゃ（2.5個分）よりはっきり大きく戻しますが、画面上端まで吹き飛ぶほどの強さにはしていません。
- 合成速度の上限は15px/step（全体上限24px/stepの半分強）に抑え、盤外へ飛ばす力にならないようにしています。
- 同じボールへの連続発動を防ぐため、800msの個別クールダウンを設けています。これが「同じ場所で無限に跳ね続けない」の主な安全装置です（ボールが跳ねるたびに横方向へも動くため、クールダウンが明ける頃には別の位置にいることがほとんどです）。
- すでに上向き速度がおよそ6.5px/step以上のボールは対象にせず、二重に加算しません。
- 複数のボールを毎フレーム独立に処理するため、3球が同時に触れても、それぞれ個別に安全な速度で打ち上げられます（`jumppadToy.test.ts`で検証）。

見た目は「押し出しおもちゃ」のようなバネ板ではなく、ロケット発射台のモチーフにしています。共通CSSの`--toy-pulse`（タップ・発動どちらでも短く立つ）と`--toy-scale`の仕組みをそのまま使い、実際にボールを打ち上げた瞬間だけ`data-toy-active`を立てて噴射炎を大きく見せます（`themes/spaceTheme.module.css`の`.spaceJumppad*`）。待機中も外周のリングを薄く光らせておき、「ここに当たると飛びそう」と常に分かるようにしています。ジャンプ台は`ToyKind`に新しい種類として追加したため、テーマ定義の`renderToy`は全テーマが`'jumppad'`にも絵を返す必要があります（`themes.test.ts`で検証）。Phase B時点では宇宙盤面だけがこの種類のおもちゃを実際に配置しており、ノーマル・海・おかしのテーマ定義には簡素な代替の絵だけを用意しています（各盤面には配置しないため実プレイでは表示されません）。

### ゴール直前の進入経路（下部の調整）

宇宙盤面を最初に作った時点（Phase B初版）では、上部・中部の派手な動きに対して盤面下部（ゴール直前）の配置がほぼ通常盤面のコピーに近く、下部へ到達した時点でボールの横位置がほぼ確定してしまい、実測で中央(1000)・右側(300/100)の得点ゾーンへほとんど到達しませんでした。バンパーの反発係数やボール速度には触れず、下部の配置だけを次のように調整しています。

- **ゴール手前の軽い千鳥配置（2段）**（`peg-space-lower-1`〜`4`）。ピッチはおよそ140〜160pxと、通常盤面よりずっと広く取り、宇宙盤面らしい空間を残しています。1段目（y=730）は中央〜右寄り（x=250, 390）だけに置き、左端（x<130程度）には置いていません。左端かつy=700〜750あたりにピンを置くと、外壁と斜めガイドAの根元（壁のすぐ内側(17,313)が高い側の端になる）が作る上部のポケットへボールが跳ね返り続け、得点ゾーンへ落ちずに上下運動を繰り返すループが実機シミュレーションで再現したため、左側は2段目（y=795, x=150, 330）と端のピンで受け止める構成にしています。
- **ゴール直前の端のピン**（`peg-space-edge-left`/`right`、x=70/410, y=825）は、通常盤面の最下段ピン（peg-row-8）と同じx座標にしています。ここをさらに外壁へ寄せると、外壁とピンの間にボールが挟まる罠ができることが実機シミュレーションで再現したため、実績のある座標へ揃えています。
- **1000点ゾーン直上の小型バンパー**（`bumper-space-goal`、x=240, y=825, 半径16）。反発係数は他のバンパーと同じ0.98のまま（弱めていません）。中央へ来たボールが毎回そのまま1000点へ直進せず、左右の300点ゾーンへ弾かれる／まれに抜けて1000点へ入る、という分岐を作っています。

「短い斜めガイド壁で右方向への経路を作る」対策も検討しましたが、下部の限られた縦幅（ジャンプ台の下端からZONE_TOPまでおよそ190px）では、既存のピン・バンパーとの間隔（半径の和＋ボール直径＋16px以上）を保ちながら追加できる余地がほとんどなく、無理に詰め込むと新しい挟まりの罠を作ってしまうため見送りました。千鳥ピン2段と小型バンパーだけで、実測上は右側ゾーンへの到達経路を作れています。

### 物理検証とプレイ時間

`pinballSimulation.ts`のヘッドレスシミュレーションへ`boardConfig: spaceBoard`を渡し、シード付き32試行（タップなし）・12試行（100ms連打）で検証しています（`pinballSimulation.test.ts`）。実測では、タップなし32試行で全試行が安全タイマーに頼らず得点確定し、1プレイの時間はおおむね7〜18秒（中央値・平均とも12〜16秒程度）に収まりました。ジャンプ台へ実際にボールが触れる頻度も別途ヘッドレス計測で確認し、大半のプレイでジャンプ台が最低1回は作動することを確認しています。

配置を決める過程で、大きな半径のバンパー（惑星バンパー、半径26）を外壁へ寄せすぎると、壁とバンパーの間のくぼみにボールが挟まって停滞ナッジでも抜けられなくなる罠ができることが実機シミュレーションで見つかりました（x=80付近に置いたところ、安全タイマー45秒に到達する試行が60試行中5件発生）。バンパーの中心xを外壁からより離す（x=110/370）ことで解消しています。同様に、斜めガイド壁のすぐそばへピンを置いたことで、ピンと壁のあいだにボールが挟まる罠が再現したため、該当のピンは取り除いています。ジャンプ台の打ち上げ方向を「中心から見た当たった側へ押し出す」向きに固定していたときは、盤面構造（斜めガイド壁の向き）による片寄りをジャンプ台自身が助長し、得点ゾーンの分布が大きく片側へ偏ったため、打ち上げの左右方向は完全にランダムへ変更しています。ゴール直前の下部配置を調整したあとも、990球規模（複数のシード範囲・タップなし／600ms間隔／100ms連打の組み合わせ）でヘッドレス検証をやり直し、安全タイマーへ到達した試行が0件であること、5つの得点ゾーンすべてに実際にボールが入ること（1000点・右300点・右100点を含む）を確認しています。

## 海盤面（oceanBoard）とシーソーtoy

Phase Cで、海テーマ（`ocean`）だけに専用の盤面配置（`boardConfigs/oceanBoard.ts`）を作りました。宇宙テーマが「重力に振り回される上下運動」を主役にしたのに対し、海テーマは「波や潮に流されながら、右→左→右と大きく蛇行して落ちていく」横方向の動きを主役にしています。基本の重力・ボール物理・得点ルール・射出タイミングは共通のまま、配置だけで印象を変えている点は宇宙盤面と同じです。

障害物は基準盤面の34個よりずっと少ない8個の散らしピンに絞り、代わりに長さ110〜240pxの斜めガイド壁4枚（`wall-ocean-ramp-a`/`-b`/`-c-left`/`-c-right`）で「右→左→（シーソーで再分岐）→右」のS字を作ります。ガイドAが射出直後を右へ、ガイドBがシーソーの手前まで左へ運び、シーソーで再び左右へ分かれたボールをガイドC-左／C-右がそれぞれ受けて次の流れへ渡します。宇宙盤面のガイド壁2枚と同じ考え方で、隣り合う壁同士・壁の端点と外壁のあいだにボール直径ぶん以上の間隔を必ず確保し、2つの静的な面が近い角度で交わる「挟まりの罠」を作らないようにしています（後述）。

### シーソー（`seesaw`）

海テーマ専用の新しいおもちゃとして「シーソー」（`toy-ocean-seesaw`）を追加しました。回転・押し上げ・ジャンプ台のように既存のMatter Bodyをそのまま使うのではなく、「板の上に乗っているボールの位置」から目標角度を毎フレーム計算し、その角度へ一定の速さで近づける完全に管理下に置いた角度制御（`seesawToy.ts`）にしています。Matterのconstraint（関節）で物理的に実装する案も検討しましたが、3球同時載荷や連続衝突での発散・脱調のリスクを避け、「最大角度を絶対に超えない」を構造で保証するため、この方式を選びました。

- 板の左右どちらに乗ったボールも、支点(`placement.x, placement.y`)からの符号付きオフセットを正規化した値の合計から目標角度を求めます。複数球が同じ側に乗ると目標角度は大きくなりますが、最大傾斜角`MAX_ANGLE=0.32rad`（約18度）で必ず頭打ちになり、何球乗っても超えません。
- 目標角度へは`ANGLE_SLEW_RATE = MAX_ANGLE / 240`（0→最大角度を約240msで駆け抜ける速さ）で近づけます。子どもが見て「乗ったら板がガタンと動いた」と分かる速さにしつつ、急激な回転でボールを吹き飛ばさない範囲に収めています。
- ボールが板の作用範囲から離れると、目標角度は0（水平）に戻り、同じ速さで水平へ戻ろうとします。
- 板の実際の角度・作用範囲の判定は、現在の傾き（`currentAngle`）で回転させたローカル座標で行うため、傾いた状態でも「板のどちら側に、どれくらい乗っているか」を正しく求められます。
- 静的BodyはEngine.updateの積分対象外ですが、`Body.setAngularVelocity`で角度変化ぶんの角速度を明示的に与えるため、matter-jsの衝突解決にも板の動きが正しく伝わります（回転おもちゃ`spinnerToy.ts`と同じ理由）。
- タップ（`activate`）は見た目のパルスだけを起こし、角度を直接操作しません。傾きは常にボールの位置から自然に決まるようにしています。

板が傾くとボールは重力に従って自然に転がり落ちるため、板の上で永久に静止し続けることはありません（`seesawToy.test.ts`で、傾いた板上のボールが有限時間で得点ゾーンの高さへ到達することを検証）。板のちょうど支点付近で釣り合うような極端な初期条件でも、盤面全体で共通の停滞ナッジ（1.5秒静止でランダムな方向へ小さく突く仕組み）が働くため、詰まりません。

見た目は木の板が岩（支点）の上で傾く「浮き橋」のイメージにしています（`themes/oceanTheme.module.css`の`.oceanSeesaw*`）。支点の岩だけは板の傾き(`--toy-spin`)を打ち消す向きへ逆回転させ、実際の物理的な支点が動かないことに合わせて常に水平を保ちます。共通CSSの`.toyVisual[data-toy-kind='seesaw']`は回転おもちゃと同じ理由（丸いパッドではなく横長の板のため）で共通の円形背景・枠を外し、見た目は完全にテーマ側のDOM要素で描きます。シーソーは`ToyKind`に新しい種類として追加したため、テーマ定義の`renderToy`は全テーマが`'seesaw'`にも絵を返す必要があります（`themes.test.ts`で検証）。Phase C時点では海盤面だけがこの種類のおもちゃを実際に配置しており、ノーマル・宇宙・おかしのテーマ定義には簡素な代替の絵だけを用意しています。

### 潮流としての押し出しtoy（`launcherTide`）

海テーマでは、既存の押し出しtoy（`launcher`、`launcherToy.ts`）を「潮流」として2個（`toy-ocean-tide-right`/`toy-ocean-tide-left`）配置しています。タップされたときだけ働く点、有効時間・クールダウンなどの安全装置は他テーマと変わりません。異なるのは、`ToyPlacement`へ新しく追加した任意設定`launcherTide`（`toyLayout.ts`）を海テーマの配置だけに与えている点です。

```ts
launcherTide?: {
  readonly biasDirection: 1 | -1        // 押し出す向き（1=右、-1=左）
  readonly upSpeedScale?: number        // 上向きの勢いの倍率（既定1）
  readonly horizontalSpeedScale?: number // 横方向の勢いの倍率（既定1）
}
```

`launcherTide`が未指定のときは`launcherToy.ts`の挙動は一切変わらないため、通常・宇宙・おかしテーマの押し出しtoyには影響しません。海テーマの2個は`upSpeedScale: 0.3, horizontalSpeedScale: 3.2`を指定しており、「打ち上げる」感触ではなく「押し流す」感触になるよう、上向きの勢いを大きく弱め、横方向の勢いを強めています。向きは`biasDirection`の方向へ75%の確率（`TIDE_BIAS_PROBABILITY`）で偏らせ、残り25%は逆向きにも散らします。100%その方向へ固定すると「ボールを完全に決まったルートへ強制する」ことになってしまうため、当たり方やタイミングによって「潮に乗る」「潮を外れる」の両方が起こる余地を残しています。

upSpeedScaleを0.3まで弱めているのは、100ms間隔の連打をヘッドレスシミュレーションで検証した結果です。0.55程度だと、常時「発動中」になったtoyの作用範囲にボールが戻ってくるたびに何度も打ち上げられ、斜めガイドA/Bのあいだを行ったり来たりし続けて安全タイマー（45秒）に達する試行が多発しました。上向きを弱め、横方向を強めることで、毎回のタップでボールをしっかり横へ逃がし、この往復ループを解消しています。

### 物理検証とプレイ時間

盤面の座標は決め打ちで終わらせず、`pinballSimulation.ts`のヘッドレスシミュレーションを数百試行流して安全タイマーに頼らず完了することを確認しながら調整しました。「静的な壁同士・壁と障害物・障害物とtoyの間にボール直径ぶんの余裕（隙間64px以上）がないと、幾何学的にボールが動けなくなる一点が生まれる」という罠（通常盤面のCORNER_ESCAPE_ZONESと同種の問題）が、斜めガイド壁の端点同士、壁の端点と外壁のあいだ、押し出しtoyと外壁のあいだなど、盤面のあちこちで再現しました。壁の座標そのものを調整して隙間を作ることで解決しています（`boardConfigs/oceanBoard.test.ts`で、斜めガイド壁同士・壁の端点と外壁・障害物やtoyと壁のあいだの最短距離を検証）。

`pinballSimulation.test.ts`へ`boardConfig: oceanBoard`を渡す海盤面専用のシナリオを追加し、シード付き32試行（タップなし）・12試行（100ms連打）で検証しています。実測では、タップなし32試行で全試行が安全タイマーに頼らず得点確定し、1プレイの時間はおおむね8〜16秒（中央値11秒程度）に収まりました。100ms連打でも全試行が安全タイマーに頼らず完了します。3球同時射出でも複数球が同時に盤面上に存在すること、5つの得点ゾーンすべてに実際にボールが入ること（複数のシード範囲で確認）も検証しています。

## 得点ゾーンと得点確定

盤面下部は`ZONE_TOP=875`から下端までの125pxを5つの得点ゾーンに等分し、得点は中央が最も高くなります（`SCORE_ZONES`: 100 / 300 / 1000 / 300 / 100）。

ボールが得点ゾーンのセンサーに触れる、または盤外へ脱出する（救済判定）と、そのballIndexの得点が確定します。

得点は`scoring.ts`の`PinballScoreState`が持ちます。`recordBallScore`は「既に確定済みのballIndexは変更しない」ことを保証しており、物理エンジン側から同じ球のセンサー通過イベントが複数回届いても二重加算されません。1球ごとの得点確定は`usePinballEngine`側の`finalizeBall`でもガードしており、二重防御になっています。

## 終了保証

3球すべての得点が確定すると`onFinished`が一度だけ呼ばれます。得点確定・ゴール判定そのものはこの時点で完了しており、`FlagPinballPlay.tsx`の`FINISH_TRANSITION_DELAY_MS=1500ms`は「結果画面への画面遷移だけ」を遅らせるものです（`onFinished`後に`setTimeout`で一度だけ`navigate`を呼ぶ、既存の得点確定ロジックに触れない構造）。得点ポップ（900ms表示）が最後まで見え、かつ最後のボールが盤面へ着地する余韻も少し見えるようにするための待ち時間で、この間におもちゃをタップしても`finishTimeoutRef`は参照されないため、連打しても遷移が延長されたりゲームが終わらなくなったりしません。通常のプレイでは発動しない想定の安全タイマー（射出から45秒）も備えており、ボールが引っかかって止まったままになる異常系でも必ずゲームが終了します。

## プレイ時間・速度の測定

`pinballSimulation.ts`は盤面・物理定数・おもちゃのBodyを共有し、rAFなしの固定タイムステップでmatter-jsのワールドをヘッドレスに回します。`toyTapIntervalMs`で全おもちゃをタップする間隔を指定でき、`null`なら一度もタップしません。1球目の射出から3球目の得点確定までを、3球・通常モード・`SEED_BASE=0x1f2e3d4c`、`SEED_STEP=7919`の32試行で測定した実測値は次の通りです。得点確定したゾーンのidは`scoredZoneIds`に`scoreSteps`と同じ順序で記録します。

| シナリオ | min | 中央値 | 平均 | max |
| --- | ---: | ---: | ---: | ---: |
| おもちゃ無し（従来） | 7.13秒 | 10.75秒 | 10.91秒 | 16.93秒 |
| おもちゃ有り・一度もタップしない | 6.27秒 | 10.62秒 | 11.20秒 | 17.87秒 |
| おもちゃを600msごとにタップ | 7.78秒 | 11.98秒 | 12.75秒 | 21.77秒 |
| おもちゃを100msごとに連打 | 7.02秒 | 12.83秒 | 14.04秒 | 30.07秒 |

得点ゾーンの確定数（100点 / 300点 / 1000点、32試行×3球=96球）の参考値は次の通りです。

| シナリオ | 100点 | 300点 | 1000点 | 合計 |
| --- | ---: | ---: | ---: | ---: |
| おもちゃ無し（従来） | 39 | 33 | 24 | 96 |
| おもちゃ有り・一度もタップしない | 46 | 21 | 29 | 96 |
| おもちゃを100msごとに連打 | 52 | 25 | 19 | 96 |

一度もタップしない場合は、中央値・平均と得点ゾーン分布が従来と大きく変わらず、既存のプレイ時間と得点体験を壊していません。タップすると得点分布の変化とプレイ時間の伸びが生まれますが、600msごとのタップでも最大21.77秒、100ms連打でも最大30.07秒で、45秒の安全タイマーには到達せずゲームは自然に終わります。`pinballSimulation.test.ts`も100ms連打を回帰シナリオとして実行し、全試行が完了すること、`usedSafetyTimeout === false`であること、固定ステップから算出した時間と一致することを継続的に検証しています。

別途、1球が`ZONE_TOP`を通過するまでをシード付き20試行し、各固定ステップの速度（logical px/s）とピン衝突を集計しました。試行ごとの平均速度の平均は266.96px/s、試行ごとの最大速度の平均は593.29px/s、ピン衝突回数の平均は18.35回/球でした。

## レスポンシブ

盤面の拡縮は`useBoardScale`が担当し、盤面を置く領域の幅・高さそれぞれから求めた倍率の小さい方（上限1.6倍）を`transform: scale()`で掛けます。このとき`PinballBoard`の外枠は`position: absolute; inset: 0`で領域を埋めます。`height: 100%`にすると、親がフレックスで高さを与えていてもブラウザが百分率の基準を確定できず「盤面自身の高さ(1000px)」が採用され、その値を計測して倍率を決めるために倍率が1から動かない（＝タブレットで盤面が拡大されない）循環参照が起きるためです。

実測値は次の通りです。safe-areaを0、ページ左右paddingを8px、ボード周辺gapを4pxとして計算しています。

| 画面 | boardArea | scale | 表示盤面 |
| --- | ---: | ---: | ---: |
| 375×812 | 359×744 | 0.744 | 357×744 |
| 390×844 | 374×776 | 0.776 | 372×776 |
| 360×800 | 344×732 | 0.717 | 344×717 |
| 1024×768 | 1008×700 | 0.700 | 336×700 |

プレイ画面は盤面の最大化を優先し、ヘッダ（3球の小さい国旗ボールと得点）を最小限の高さに詰めます。選択画面のグリッドは`repeat(auto-fit, minmax(...))`で画面幅に応じて4〜6列に自動調整し、「あそぶ！」ボタンは`position: sticky`で常に画面下部に表示します。結果画面は縦に低い画面（高さ820px以下）で見出し「けっか」を省いて補助2ボタンを横並びにし、さらに横向きの低い画面（`orientation: landscape` かつ高さ560px以下）では得点まとめと操作ボタンを左右2カラムに切り替えます。合計得点は「ごうけい」と点数を別要素の2行に分け、狭い画面でも読みにくい折り返しが起きないようにしています。

## 効果音

`src/utils/quizSound.ts`にWeb Audio APIで合成する専用の効果音（`playPinballLaunchSound`/`playPinballBumperSound`/`playPinballScoreSound`/`playPinballTotalSound`）を追加しています。音声ファイルは追加していません。選択画面の「あそぶ！」押下時に`primeAudio()`を呼び、iOS Safariでも以降の効果音が確実に鳴るようにしています。

障害物が増えたため、同一障害物ごとの120msクールダウンに加えて全障害物共通の70msクールダウンを設け、別のピンへの連続衝突でも音が重なり続けないようにしています。

## テスト

物理エンジン（matter-js）はjsdomでは動かさず、`usePinballEngine`を`vi.mock`で差し替えて、テストからは`onBallScored`/`onFinished`を直接発火させることでゲーム進行（選択→プレイ→結果）だけを検証します（`FlagPinball.test.tsx`）。ここでは、`onFinished`直後は結果画面へ即座に遷移しないこと、`FINISH_TRANSITION_DELAY_MS(1500ms)`ちょうどで遷移すること、その待ち時間中に回転おもちゃを連打しても遷移が延長されないことも確認しています。盤面レイアウト・得点計算・選択ロジック・型ガードは、それぞれ`boardLayout.test.ts`/`scoring.test.ts`/`selection.test.ts`/`playState.test.ts`で純粋関数として単体テストしています。盤面レイアウトでは障害物間隔の「半径の和＋ボール直径＋16px」や千鳥配置の性質を検証し、`data/pinballFlags.test.ts`では`PINBALL_FLAG_IDS`の全SVGが4:3であることを検証します。

テーマ定義は`themes/themes.test.ts`で4テーマの登録順、id・絵文字・表示名の重複なし、盤面・おもちゃのクラス名、2種類のおもちゃの見た目、未知idなどのノーマルへのフォールバックを検証しています。また、テーマ定義に盤面座標・得点・物理のプロパティがないことも検証します。`themeStore.test.ts`では既定値、localStorageの読み書き、テーマ変更時だけの購読通知、未知・空文字のフォールバック、localStorageの読み書き例外時の継続動作を確認します。`PinballThemePicker.test.tsx`では既定表示、左右ボタンによる循環、絵文字・テーマ名、保存値、4テーマの登録順、ボタンのアクセシブルネームを検証します。

おもちゃの配置は`toyLayout.test.ts`でidの重複、盤面内のボール半径ぶんの余裕、全障害物との距離、判定円の非重複、得点ゾーン外、0.70倍でも44px以上のタップ直径に加え、左右2個の回転おもちゃが存在すること・右Toyが左Toyの盤面中心に対する鏡写しの位置にあること・左右でサイズに差がないこと・idとラベルが別々であることを検証します。`spinnerToy.test.ts`では作用範囲内の停滞球への物理効果、範囲外への無作用、停止時に速度を制限しないこと、1タップの効果時間、再タップで減速しないこと・効果時間が延長されること、連打しても角速度上限を超えないこと、静的Bodyの中心同期、compound bodyが十字であること、発動終了、見た目の角度同期、ボールが得点ゾーン方向へ抜けることを固定ステップで確認するほか、左右2個のToyを独立インスタンスとして生成し、片方だけ発動させても他方の状態へ漏れないこと・両方発動したときの角速度上限が同じであること・見た目の羽根寸法が左右で一致することを検証します。`launcherToy.test.ts`では有効状態になること・一定時間続くこと、有効時間中にあとから来たボールが跳ね上がること、未タップ・期限切れ後は作用しないこと、作用範囲・上方除外・得点ゾーン直前の除外、上向き速度と合成速度の上限、ボールごと1200msのクールダウン、複数球への作用、拡大と当たり判定（`circleRadius`）が同期し往復で誤差が出ないこと、空振り時の見た目、連打しても有限時間で進むこと、左右の散らし、新しいランタイムが初期状態を持つこと（リスタート時に状態が残らないこと）を検証します。`PinballToy.test.tsx`ではポインタ操作の二重発動防止、Enter/Space操作、0.70倍でも64px以上のタップ領域を検証します。

`pinballSimulation.test.ts`では、おもちゃをタップしないシード付き32試行と、100ms連打の12試行を固定タイムステップで回し、完了、`usedSafetyTimeout === false`、時間計算の整合性を検証します。追加したテストでは`scoredZoneIds`の長さが3球と一致し、すべて`SCORE_ZONES`に存在するidであることを検証します。100ms連打では最大時間が40秒未満であることも境界として確認します。全射出モードでは全旗を射出して全球が得点確定し、安全タイマーを使わず、複数球が同時に盤面上に存在することを検証します。同じファイルへ`boardConfig: spaceBoard`/`boardConfig: oceanBoard`を渡す宇宙盤面・海盤面専用のシナリオも追加し、タップなし32試行・100ms連打12試行のどちらも安全タイマーに頼らず完了すること、3球が同時に盤面上へ存在すること、`scoredZoneIds`が既知のゾーンIDだけになること、5つの得点ゾーンすべてに実際に到達することを検証しています。

Phase Bで追加・変更したテストは次の通りです。`boardConfigs/index.test.ts`は「Phase A時点では宇宙・海・おかしが通常盤面と一致する」という検証を「海・おかしは一致するが、宇宙は専用配置で不一致」へ更新しました。`boardConfigs/spaceBoard.test.ts`は`normalBoard.test.ts`と同じ観点（障害物間隔・盤面内・タップ判定の非重複など）に加え、宇宙専用の斜めガイド壁2枚が交わる隙間を作らないこと（高さが十分ずれていること）、外壁・射出ガイド壁が通常盤面と同じ形状のままであること、ジャンプ台が斜めガイド壁と重ならないことを検証します。`jumppadToy.test.ts`は、タップなしで作用範囲内のボールを自動で打ち上げること、範囲外には無作用であること、上昇速度が暴走しないこと、個別クールダウン中は連続で打ち上げないこと・クールダウンが明ければ再び打ち上げられること、複数球が同時に触れても例外を投げず全球へ安全に作用すること、すでに上昇中のボールへ二重作用しないこと、タップ時のパルス・実発動時のactive状態、得点ゾーンへ有限時間で到達すること、新しいランタイムの初期状態を検証します。`themes/themes.test.ts`は検証対象の種類へ`'jumppad'`を加え、4テーマすべてが例外なく絵を返すことを確認します。

Phase Cで追加・変更したテストは次の通りです。`boardConfigs/index.test.ts`はさらに「おかしのみが通常盤面と一致し、宇宙・海はどちらも専用配置で不一致」へ更新しました。`boardConfigs/oceanBoard.test.ts`は`spaceBoard.test.ts`と同じ観点に加え、4枚の斜めガイド壁が互いに（端点同士の実距離で）ボール直径ぶん以上離れていること、壁の端点が外壁からも十分離れていること、半径20px以上の障害物・toyが外壁・斜めガイド壁のどちらからも十分離れていること、シーソーが横長の板として最大傾斜時の広がりを含めても斜めガイド壁と十分離れていることを検証します。`seesawToy.test.ts`は、板の右端寄り／左端寄りに乗ったボールでそれぞれ逆符号（右が下がる／左が下がる）に傾くこと、作用範囲外には無作用であること、最大角度で頭打ちになり異常回転・360度回転をしないこと、ボールが離れると水平へ戻ろうとすること、傾いた板上でボールの速度が恒久的にゼロへ固定されないこと（永久停止しないこと）、3球同時に乗っても例外を投げず角度が範囲内に収まること、同じ側に複数球が乗っても頭打ちを保つこと、タップが角度を変化させず見た目のパルスだけを起こすこと、得点ゾーンへ有限時間で到達すること、新しいランタイムの初期状態（水平・非アクティブ）を検証します。`themes/themes.test.ts`は検証対象の種類へ`'seesaw'`を加えました。`launcherToy.ts`へ追加した`launcherTide`設定は、未指定時に既存の挙動（左右完全ランダム・上向き主体）から変わらないことを`oceanBoard.test.ts`・既存の`launcherToy.test.ts`の回帰で担保しています。

## `ToyRuntime`を使った将来の拡張ポイント

現在のおもちゃ追加の拡張点は`ToyRuntime`です。新しいタップおもちゃや、将来フリッパーのような動的な物理装置を加える場合も、エンジンへ個別の分岐を増やすのではなく、次の接続点を使います。

- `toyLayout.ts`に配置・半径・タップ判定半径・ラベルを追加し、`ToyKind`へ新しい種類を登録する。配置は既存障害物、得点ゾーン、他のおもちゃとの距離をテストで確認する。
- 新しい`<kind>Toy.ts`に`ToyRuntime`（物理Body、`activate`、毎フレームの`update`、`readVisualState`）を実装し、`toyRuntime.ts`の`createToyRuntime`を唯一の種類分岐として追加する。速度設定、作用範囲、クールダウン、終了性の安全装置はこのランタイム内へ閉じ込める。
- 各テーマの`renderToy`とCSS Modulesへ見た目だけを追加する。テーマ側へ物理や座標を複製せず、同じランタイムを4テーマで共有する。
- `usePinballEngine.ts`は現在と同じく、登録・発動・毎フレーム更新・見た目のDOM反映・cleanupを`ToyRuntime`共通処理として扱う。キーボードを含む操作ボタンのタップ領域と誤操作防止は`MINIGAME_DEVELOPMENT_GUIDELINES.md` §4に沿って確認する。
- ジャンプ台（`jumppad`）はこの拡張点を使って追加した最初の「タップ待ちしないおもちゃ」の実例です。`activate`をタップ演出だけに使い、実際の物理効果は`update`が毎フレーム自律的に判定する形にすれば、エンジン側の分岐を増やさずに「自動作動するおもちゃ」も同じ`ToyRuntime`で表現できます。
- シーソー（`seesaw`）はこの拡張点を使って追加した、静的Bodyの角度を毎フレーム計算で管理する「動的だが完全に管理下にあるおもちゃ」の実例です。Matterのconstraint（関節）で物理的に実装せず、角度そのものを直接計算・設定する方式にすることで、複数球同時載荷でも発散せず、最大角度を構造で保証できます。回転おもちゃ（`spinnerToy.ts`）の「静的Bodyの角度をBody.setAngleで直接操作し、Body.setAngularVelocityで衝突解決に伝える」という既存パターンをそのまま踏襲しています。
- 押し出しtoy（`launcher`）は、`ToyPlacement`へkind別の任意設定（`launcherTide`）を持たせることでテーマごとに挙動を調整できることを示す実例にもなりました。同じランタイムを保ったまま、配置データだけでテーマ間の差を作れる拡張方法は、今後別のtoyでも使えます。未指定時に既存の挙動から一切変わらないことをテストで担保するのが前提条件です。
- 今後ハンマー・ゲートなどを追加する際も、タップ式・自動式・動的角度制御のどれであれ、この4つの接続点（配置・ランタイム・見た目・エンジンの共通処理）だけで完結させることを目指してください。
